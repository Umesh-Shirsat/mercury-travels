{
  "name": "mercury-travels",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:run": "cypress run",
    "cy:all": "cypress run --spec './cypress/e2e/feature/mercury-travels.feature' --headed --browser chrome --no-exit",
    "cy:parallel": "cypress-parallel -s cy:run -t 2 -d ./cypress/e2e/feature/ -a '\"--headed --browser chrome --no-exit\"'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^20.1.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.1",
    "cypress": "^13.13.0",
    "cypress-parallel": "^0.14.0"
  },
  "devDependencies": {
    "allure-commandline": "^2.29.0",
    "allure-cypress": "^3.0.0-beta.6"
  }
}
